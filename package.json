{
  "scripts": {
    "check": "prettier --check '**/*.{js,css,md,html}'",
    "format": "prettier --write ."
  },
  "dependencies": {
    "prettier": "^3.3.3",
    "@shopify/prettier-plugin-liquid": "^1.5.0"
  }
}
